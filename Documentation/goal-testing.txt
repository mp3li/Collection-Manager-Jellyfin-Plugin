COLLECTION MANAGER — PRIVATE TESTING TRACKER
===================================================

Use this tracker for real private Jellyfin 10.11.11 testing. Do not put test
instructions in project-goals.txt. Do not record server URLs, usernames,
credentials, API keys, private media titles, paths, NFO contents, logs, or
screenshots containing private information here.

Automated source checks
-----------------------
[x] Release build uses Jellyfin.Controller 10.11.11 and Jellyfin.Model 10.11.11.
[x] Release build completes with zero warnings and zero errors.
[x] Embedded dashboard JavaScript passes syntax validation.
[x] `git diff --check` reports no whitespace errors for the release changes.
[ ] Before a release, audit staged public files for credentials, private paths, media, NFOs, logs,
    build output, .DS_Store files, test artifacts, or release archives are staged.

Private installation and dashboard availability
-----------------------------------------------
[x] Build the exact release ZIP.
[x] Create a manifest using the exact release ZIP checksum and published source URL.
[ ] Add the manifest to a private Jellyfin 10.11.11 server.
[ ] Install the plugin through Jellyfin's repository interface.
[ ] Restart Jellyfin and confirm Collection Manager appears in Dashboard.
[ ] Open the plugin dashboard as an administrator and confirm all eleven tabs load.
[ ] Confirm a manifest update detects and offers a newer compatible release.
[ ] Confirm every dashboard tab uses the same tab shape as Media Tagging
    Manager, including the inactive tab background, active fill, and squared
    lower tab corners.
[ ] Compare equivalent dashboard sections at the same viewport with Media
    Tagging Manager: section alignment, heading hierarchy, spacing, controls,
    and themed dialogs must use the same shared visual system.

Main Settings
-------------
[ ] Library picker lists the expected Jellyfin libraries.
[ ] Select All and Select None update the picker correctly.
[ ] Save Libraries persists the selected library list after leaving and reopening the page.
[ ] Save Main Settings persists libraries and the newly-added-media preference after a Jellyfin restart.
[ ] The newly-added-media preference is retained without modifying media metadata.
[ ] Scan is unavailable until at least one library is saved.
[ ] A scan shows a progressing status and completes without exposing media paths.
[ ] Starting a second scan while one is running does not start a conflicting scan.
[ ] My Metadata Tags Overview starts collapsed.
[ ] Opening the overview displays "Showing Last Available Scan" with the
    completed scan's local date and time.
[ ] Opening the overview expands the last-scan library sections by default,
    even when the current saved-library selection has since changed.
[ ] Each overview library can be collapsed and expanded independently.
[ ] Each overview metadata type is its own independently expandable tag picker
    showing actual metadata values, never media-title rows or placeholders.
[ ] Overview tag searching, alphabetical ordering, matching-media counts, and
    fifty-tag Previous/Next pagination work correctly within the opened type.
[ ] Opening a small type such as Ratings returns its tags promptly without
    processing the entire scanned library again.
[ ] After a Jellyfin restart, the last completed scan and its timestamp remain
    available without starting a new scan.
[ ] Select All, Select None, and Save Libraries appear together after the
    library picker, in that order.
[ ] Cleanup performs only the selected actions, reports a result, and does not
    remove media files or alter library metadata.

Catalog fidelity and scale
--------------------------
[ ] Confirm ordinary Jellyfin tags, Genres, Providers, Networks, Studios,
    Production Companies, Production Countries, Cast, Crew, Languages, Ratings,
    Keywords, Existing Collection Metadata, years, direct Jellyfin fields, and
    local NFO fields appear when present.
[ ] Confirm Provider and Network appear as separate metadata types.
[ ] Confirm metadata assigned by Media Tagging Manager appears alongside, not
    instead of, all other existing library metadata.
[ ] Confirm values are alphabetically sorted case-insensitively.
[ ] Confirm each metadata-type selector starts collapsed.
[ ] Confirm a metadata type's values are not requested until it is expanded.
[ ] Confirm searching filters only the expanded type and resets to page one.
[ ] Confirm each value page contains at most fifty values and Previous/Next and
    current/total page controls are correct.
[ ] Confirm displayed matching counts equal the selected library's scanned catalog.
[ ] Confirm Cast and Crew-related values show a person image only when Jellyfin
    has a primary person image.
[ ] Exercise a library with more than ten media items and a metadata type with
    more than fifty values; the dashboard must remain responsive.

Create Manual Collections
-------------------------
[ ] Collection Overview & Editor appears in the same styled secondary tab row
    used by Media Tagging Manager, below the primary row. Create Manual
    Collections remains in the primary tab row, and every Jellyfin library is
    initially collapsed.
[ ] Expanding a library loads only that library's current selectable media and
    displays the same poster, title, type, year, and overview details used by
    Collections Overview & Editor, plus a picker checkbox and no edit button.
[ ] A television library shows each Series once and never offers individual
    episodes, seasons, folders, deleted scenes, or other extras as choices.
[ ] A movie library shows Movies only; folders and unrelated item kinds are not
    available as manual collection choices.
[ ] A music library presents Audio as Audio and MusicAlbum as Audio Album, without
    folders or unrelated media kinds.
[ ] A book library presents only Book and Audiobook items. Rich-text markup and
    encoded entities in a book description display as readable plain text.
[ ] A Home Videos or Live Performance library presents only its applicable
    Movie or Video items, never folders or Jellyfin video extras.
[ ] Each expanded manual library picker shows a Search media field, no more
    than ten media items per page, and a small Page _ out of _ indicator.
[ ] Manual-library Previous and Next controls change only that library's page
    and do not move the dashboard scroll position.
[ ] Manual-library Select All selects every loaded choice in that library,
    including choices on other pages; Select None clears every choice in that
    library. Both controls appear between the page controls.
[ ] Select media from more than one library and confirm all selected items
    appear together in the singular Individual Collection Draft section.
[ ] Confirm the editable collection title appears above the draft media and its
    count says exactly how many selected media items will be included.
[ ] Remove an item from the draft and confirm it is immediately removed from
    the draft and unchecked in the already-open library picker.
[ ] Creating a titled draft creates one ordinary native Jellyfin collection
    containing every selected item, then clears the manual title and selections.
[ ] Attempt creation without a title and without selected media; neither case
    creates a native collection.

Individual Tag Collections
--------------------------
[ ] Selecting a value immediately creates one draft; unselecting it removes that draft.
[ ] A draft defaults its title to the selected metadata value and accepts edits.
[ ] Draft Create this draft checkbox controls whether it is created in a batch.
[ ] A draft's source library is always included.
[ ] Select All and Select None affect only the draft's other saved libraries.
[ ] Changing additional libraries immediately updates the matching count,
    explanation, and grouped preview.
[ ] The included-media list starts collapsed and expands grouped by library.
[ ] Create Selected Collections reports current draft, completed/total progress,
    and Created, Matched an existing collection, Skipped, or Failed for each draft.
[ ] A same-title conflict is shown in-page, never in a browser confirmation dialog.
[ ] Use existing collection leaves the native collection unchanged.
[ ] Skip does not create or alter a collection.
[ ] Editing a conflicting title allows normal creation when no conflict remains.

Combined Tag Collections
------------------------
[ ] The tab uses the same collapsed library/type picker, lazy loading, search,
    alphabetical ordering, counts, person images, and fifty-value paging.
[ ] Selecting multiple values creates one combined draft, not multiple drafts.
[ ] Every selected-tag row shows source library, type, value, source count, and removal.
[ ] Source libraries are always included in the shared scope and cannot be removed.
[ ] Select All and Select None apply only to other saved libraries.
[ ] Changing the shared additional-library scope immediately updates the preview.
[ ] A union scenario includes every media item matching any selected tag.
[ ] A media item matching two selected tags appears once in the total and preview.
[ ] The preview starts collapsed and expands with media grouped by library.
[ ] A title and at least two tags are required.
[ ] Creation shows the collection currently being created, unique item count, and result.
[ ] Same-title handling is visible in-page and does not change an existing collection.
[ ] A successful creation clears the combined draft and preserves the completion result.

Multi-Match Tag Collections
---------------------------
[ ] The tab uses the same collapsed library/type picker, lazy loading, search,
    alphabetical ordering, counts, person images, and fifty-value paging.
[ ] Selecting multiple values creates one multi-match draft, not multiple drafts.
[ ] Every selected-tag row shows source library, type, value, source count, and removal.
[ ] Source libraries are always included in the shared scope and cannot be removed.
[ ] Select All and Select None apply only to other saved libraries.
[ ] Changing the shared additional-library scope immediately updates the preview.
[ ] An intersection scenario includes only media matching every selected tag.
[ ] Tags selected from different source libraries use the shared source-plus-additional scope.
[ ] A zero-match state clearly explains that no media matches every selected tag.
[ ] Creation is disabled for a zero-match state, a missing title, or fewer than two tags.
[ ] The preview starts collapsed and expands with media grouped by library.
[ ] Same-title handling is visible in-page and does not change an existing collection.
[ ] A successful creation clears the multi-match draft and preserves the completion result.

Collection Overview & Editor
----------------------------
[ ] Selected Collections Settings saves either collection type individually or both types together.
[ ] Scan for Collections reports progress, a found-collection result, and the local last-scan date and time.
[ ] The saved flat all-collections overview remains available after leaving, reopening, and restarting Jellyfin without another scan.
[ ] A new scan regenerates the saved overview using all current native Jellyfin collections, without library sub-sections.
[ ] The Collections Overview & Editor starts collapsed and shows no library sub-sections.
[ ] The flat overview has no collection pagination or collection-count/page-size
    summary; all entries start collapsed and can be opened independently.
[ ] Newly added collection names and media titles use the selected added color; newly removed names and titles use the selected removed color.
[ ] The first-ever scan is a neutral baseline: existing collection names and media titles are not shown in the added color.
[ ] Overview color choices persist after reopening the dashboard.
[ ] Collection media in the overview uses Jellyfin posters when a primary image is available.
[ ] Edit Collection opens the correct titled dialog with a current collection title and item count.
[ ] The editor dialog's background, text, labels, and controls are readable in the active Jellyfin dashboard theme.
[ ] Media in collection loads in bounded lazy pages inside the scrollable picker and uses posters when available.
[ ] Select All and Select None appear beside the editor paging controls and operate on selected current-page and previously selected media items as intended.
[ ] Add to Collection opens a poster-capable native collection picker, shows thirty collections per page, and queues only the chosen media for the chosen collection.
[ ] Save applies the edited native collection title, selected removals, and queued additions together; Close makes no server-side collection change.
[ ] Undo Last Collection Action reverses the last recorded create, membership, or title action without touching media files.
[ ] Remove All Collections Made By This Plugin removes only collections recorded as plugin-made.
[ ] Remove All Media Additions To Existing Collections Not Made by This Plugin removes only recorded plugin additions to non-plugin collections.

Blank art tabs
--------------
[ ] Default Art Settings, Text Focused Art Settings, Poster Focused Art
    Settings, Logo Focused Art Settings, and Multi-Collection Gradient Art
    appear in that order after the existing collection tabs.
[ ] Default, Text Focused, and Poster Focused Art Settings share the second tab
    row with Collection Overview & Editor. Logo Focused Art Settings and
    Multi-Collection Gradient Art appear together in a third tab row.
[ ] The five art tabs contain no settings or placeholder content until their
    separate art-feature plans are approved.

Native Jellyfin, watcher, and privacy behavior
-----------------------------------------------
[ ] Open every created collection's Jellyfin three-dot menu and confirm native
    controls, including image/artwork controls, remain available.
[ ] Confirm the plugin does not fetch, create, edit, remove, or overwrite media metadata or NFO files.
[ ] Confirm the metadata-change listener ignores collection items and does not
    create a feedback loop when native collection metadata changes.
[ ] Confirm the newly-added-media preference persists and is respected by any
    configured automatic maintenance behavior without changing unrelated collections.
[ ] Confirm collection previews and outcomes never display private file paths.
[ ] Confirm only elevated Jellyfin administrators can access the plugin API/dashboard.

Results log
-----------
Date:
Jellyfin version:
Plugin ZIP version and checksum:
Tab or check:
Sanitized sample setup:
Observed result:
Status: [x] passed / [~] partial / [!] failed / [?] blocked
Follow-up:
